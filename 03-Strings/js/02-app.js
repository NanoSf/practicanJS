//! METODOS
const producto = 'Monitor 20 Pulgadas';
console.log(producto);

//Conocer la cantidad de letras de cadena de texto
console.log(producto.length);

//Consutal si la cadena existe y retorna la posicion donde se encuentra, retorna -1 si no la encuentra
console.log(producto.indexOf('Pulgadas'));

//Consulta si la cadena existe y retorna true o false
console.log(producto.includes('Pulgadas'));
